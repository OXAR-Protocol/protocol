node_modules/

# Native projects are generated by Capacitor (`npx cap add ios|android`) — not committed
# while we spike. Once we have signing/native tweaks worth keeping, we can track them.
ios/
android/

*.log
.DS_Store

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Node
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Next.js (web)
.next/
out/
.vercel/

# Build output
dist/
build/
*.tsbuildinfo
next-env.d.ts

# EXCEPTION: web/sdk-local/dist is a COMMITTED copy of the built SDK (keeps the
# Vercel build self-contained). Track it despite the `dist/` rule above.
!web/sdk-local/dist
!web/sdk-local/dist/**

# Rust / Anchor (contracts)
target/
.anchor/
test-ledger/

# Solana
*.so

# Coverage
coverage/
.nyc_output/

# Claude Code local artifacts
.claude/settings.local.json
.claude/tmp/
.claude/worktrees/

# GitHub org profile repo (kept separately)
.github-repo/

# Sensitive: invite keys, outreach materials, secrets
oxar-invite-keys-*.txt
outreach/*.docx
outreach/*.xlsx
*.pem
*.key
secrets/
.gstack/
.vercel

# Kora fee-payer keypairs — NEVER commit (secret)
fee-payer.json
**/fee-payer.json
